[package]
name = "agichev/opengifs"
version = "0.1.0"
description = "OpenGifs — free open GIF hosting service"
license = "GPL-3.0"

[[command]]
name = "serve"
module = "php/php:8.2-apache"
runner = "https"
args = ["-d", "document_root=public"]
